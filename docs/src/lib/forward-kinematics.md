# Forward Kinematics

*Textbook Chapter 4*

!!! tip "Robot convenience wrappers"
    For simplified calls using a [`Robot`](@ref) model (e.g., `forward_kinematics_space(robot, q)`), see [Robot Model](@ref).

```@autodocs
Modules = [ModernRoboticsBook]
Pages = ["forward_kinematics.jl"]
```
